.mycart
    .header &
        @media media-min-lg
            margin-left: 60px

        &__content
            display: none !important

    &__header a
        position: relative
        display: block
        width: 44px
        height: 47px

        .ico
            margin-top: 21px
            width: 28px
            height: 25px
            fill: var(--secundary)

        .text
            display: none

        .qtd
            position: absolute
            top: 0
            right: 0
            display: flex
            justify-content: center
            align-items: center
            width: 22px
            height: 22px
            border-radius: 50%
            background: var(--primary)
            color: var(--secundary)
            font-weight: medium
            font-size: 14px